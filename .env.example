# Copy to .env (gitignored) and adjust for local runs.
# These Postgres credentials are shared by the db container and the app.
POSTGRES_DB=dev_test
POSTGRES_USER=dev_test
POSTGRES_PASSWORD=change_me_locally

# Host port to publish the app on (container always listens on 4000).
SERVER_PORT=4000
